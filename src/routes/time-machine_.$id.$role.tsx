/**
 * STALE FILE — please delete from disk.
 *
 * The active Time Machine simulation route lives in
 * `tm-sim.$id.$role.tsx` (URL: /tm-sim/$id/$role).
 *
 * This file was renamed during a routing fix, but the Cowork
 * sandbox couldn't remove the original from the mounted folder.
 * It intentionally exports nothing so TanStack's file-based route
 * generator skips it and no `/time-machine/$id/$role` route is
 * registered.
 *
 * Safe to delete via Finder / `rm` from your terminal.
 */
export {};
