// Stub — file scheduled for removal. The Top-25 cities feature is
// being rebuilt by Lovable from scratch; nothing imports this any
// more. Kept as an empty export only so the FUSE-mounted file can
// be cleaned up by Lovable's next sync (rm is blocked over FUSE).
export {};
